# Archived prototype

The original paste-your-report prototype, kept as reference material. The product now follows SPEC.md (structured entry, curated data model); free-text report parsing is deferred (see the ROADMAP parking lot). The explanation dictionary in `data.js` is a useful starting point for the Content track.

Not wired into the build. Open `index.html` directly to run it.
